import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function RolesPage() {
  return (
    <PagePlaceholder
      title="Roles"
      description="Role definitions (Admin, Project Lead, Developer, Viewer). Built in Phase 2."
    />
  );
}
