# <Feature Name>

- **Status:** Draft | In progress | Shipped
- **Owner:** <name>
- **Related specs:** [[other-feature]]
- **Last updated:** YYYY-MM-DD

## 1. Purpose & scope

What this feature does, who uses it, and what is explicitly **out of scope**.

## 2. User stories

- As a `<role>`, I want `<goal>` so that `<reason>`.

## 3. Data model

Entities / fields this feature owns or touches; enums; relationships. Link to the Flyway
migration(s) that create them.

## 4. API contract

For each endpoint: method + path + auth + request DTO + response DTO + error codes.

| Method | Path | Auth | Request | Response | Errors |
| --- | --- | --- | --- | --- | --- |

## 5. UI / behavior

Screens, components, states (loading / empty / error), and interactions.

## 6. Business rules & validation

Constraints, per-role permissions, and edge cases.

## 7. Acceptance criteria

Given / When / Then checklist that defines "done."

- [ ] Given ..., when ..., then ...

## 8. Test cases

- **Backend:** unit (service) + integration (endpoint) cases.
- **Frontend:** Playwright scenarios.

## 9. Open questions
