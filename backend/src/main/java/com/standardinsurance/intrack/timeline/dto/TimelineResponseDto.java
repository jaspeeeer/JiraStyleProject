package com.standardinsurance.intrack.timeline.dto;

/**
 * Placeholder timeline payload. {@code status} is {@code SCAFFOLD} until the feature is built.
 */
public record TimelineResponseDto(String projectKey, String status, String message) {
}
