-- V1 baseline migration.
-- Phase 0 introduces no domain tables yet; the schema is created in Phase 1
-- (V2__init_core.sql) once the core entities are defined.
-- This file establishes the Flyway baseline so the migration history starts cleanly.

-- (intentionally empty)
